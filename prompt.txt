Fix all functional bugs in Format Studio. 
Do NOT touch CSS, mobile, design, formatter.py, supabaseClient.js.

STACK: React+Vite, Node/Express, Supabase, Razorpay

━━━ BUG 1: Dashboard count = 0 ━━━
File: client/src/components/dashboard/Overview.jsx
Fix: Fetch count directly inside component:
const { count } = await supabase
  .from('documents')
  .select('*', { count: 'exact', head: true })
  .eq('user_id', user.id)

━━━ BUG 2: Paywall not blocking ━━━
File: client/src/pages/Tool.jsx
Fix: In handleSubmit, before formatting fetch fresh count:
const { count } = await supabase
  .from('documents')
  .select('*', { count: 'exact', head: true })
  .eq('user_id', user.id)
if (userPlan === 'free' && count >= 3) {
  setStatus('idle')
  return
}

━━━ BUG 3: After payment plan not updating in UI ━━━
File: client/src/hooks/usePayment.js
Fix: After verify-payment API success:
await refreshPlanAndDocs(user.id)
then navigate to dashboard

━━━ BUG 4: Activity shows mock data ━━━
File: client/src/components/dashboard/Activity.jsx
Fix: Fetch real docs from Supabase:
supabase.from('documents').select('*')
  .eq('user_id', user.id)
  .order('created_at', { ascending: false })

━━━ BUG 5: Billing history empty ━━━
File: client/src/components/dashboard/Subscription.jsx
AND server/index.js
Fix A - server: in verify-payment after plan update add:
await supabase.from('payments').insert({
  user_id: userId,
  payment_id: razorpay_payment_id,
  amount: 19900
})
Fix B - client: fetch from payments table and display

━━━ VERIFY AFTER FIXES ━━━
1. npm run build — must pass

2. node index.js — must start
3. Format 1 doc → count shows 1 in dashboard
4. Format 3 docs free → 4th blocked by PaywallModal
5. Pay → dashboard shows Professional